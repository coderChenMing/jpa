<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence" version="2.0">
    <!--配置持久化单元节点 persistence-unit
     name:持久化单元名称
     transaction-type：配置事务管理的方式
        RESOURCE_LOCAL：本地化事务管理
        JPA：分布式事务管理

    -->
    <persistence-unit name="myJpa" transaction-type="RESOURCE_LOCAL">

        <!--需要配置Jpa的实现方式-->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <!--数据库信息-->
        <properties>
            <property name="javax.persistence.jdbc.user" value="root"/>
            <property name="javax.persistence.jdbc.password" value="123456"/>
            <property name="javax.persistence.jdbc.driver" value="com.mysql.jdbc.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://192.168.137.37:3306/jpa?characterEncoding=utf-8"/>
            <!--可选配置:jpa实现方式的配置-->
            <!--配置hibernate
            1.显示sql
            2.自动创建数据库表
                create:程序运行时创建表，如果已存在先删除在创建
                update:程序运行时创建表，如果已经存在不会创建
                none:不会创建表
            -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
        </properties>
    </persistence-unit>


</persistence>
